import { Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../../styles.ts'
import PropertiesTable from '../../../Components/properties-table'
import ReferenceList from '../../../Components/reference-list'
import ComponentInfoTable from '../../../Components/component-info-table'
import ComponentRenderTable from '../../../Components/component-render'

<Meta title="ApplicationModel/Components/DarkModeSwitch/docs" />

# DarkModeSwitch

<ComponentInfoTable
  name={'DarkModeSwitch'}
  description={`A component that allows the user to change the color scheme of the application (from the light mode to the dark mode and vice versa). Visually, it looks like a switch.`}
  source="components/dark-mode-switch"
/>

<PropertiesTable tableRows={[]} />

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
     DarkMode "1" <-- "1" Switch

     class DarkMode:::green
     class Switch:::blue

`}
/>

</S.MermaidWrapper>

<ReferenceList
  tableRows={[
    {
      name: 'Switch',
      description: 'A two-state button component.',
    },
  ]}
/>
