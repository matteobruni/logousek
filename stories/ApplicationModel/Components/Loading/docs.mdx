import { Mermaid } from 'mdx-mermaid/Mermaid'

import appConf from '../../../../constants/app-conf'
import ComponentInfoTable from '../../../Components/component-info-table'
import ReferenceList from '../../../Components/reference-list'
import PropertiesTable from '../../../Components/properties-table'

# Loading

<ComponentInfoTable
  name="Loading"
  description="The component renders the loading animation."
  source="components/pages/loading"
/>

<PropertiesTable
  tableRows={[
    {
      name: 'textType',
      type: `"primary" | "ghost"`,
      defaultValue: '',
      description: `This is a props that can set the font color.`,
      required: false,
    },
  ]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
      Loading "1" <-- "1" LoadingWrapper
      LoadingWrapper "1" <-- "1" LoadingAnimation
      LoadingWrapper "1" <-- "1" P3
      LoadingAnimation "1" <-- "n" LoadingAnimationPart

      class Loading:::green
      class LoadingWrapper
      class LoadingAnimation
      class P3:::blue

`}
/>

</S.MermaidWrapper>

<ReferenceList
  tableRows={[
    {
      name: 'P3',
      description: 'Level 3 paragraph component.',
    },
  ]}
/>
