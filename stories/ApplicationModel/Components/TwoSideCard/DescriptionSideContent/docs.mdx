import { Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../../../styles.ts'
import PropertiesTable from '../../../../Components/properties-table'
import ReferenceList from '../../../../Components/reference-list'
import ComponentInfoTable from '../../../../Components/component-info-table'
import ComponentRenderTable from '../../../../Components/component-render'

<Meta title="ApplicationModel/Components/TwoSideCard/DescriptionSideContent/docs" />

# DescriptionSideContent

<ComponentInfoTable
  name={'DescriptionSideContent'}
  description={`This is a main side of two side card.`}
  source="components/two-side-card/description-side-content"
/>

<PropertiesTable
  tableRows={[

    {
      name: 'title',
      type: `string`,
      defaultValue: '',
      description: `This is the label displayed on the card.`,
      required: true,
    },
    {
      name: 'description',
      type: `string`,
      defaultValue: '',
      description: `This is the description on the card.`,
      required: false,
    },
    {
      name: 'showMainSide',
      type: `() => void`,
      defaultValue: '',
      description: `This is a callback that is called after the user clicks on the icon.`,
      required: false,
    },

]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
     DescriptionSideContent "1" <-- "1" ActivityCard
     ActivityCard "1" <-- "1" Navbar
     ActivityCard "1" <-- "1" InformIcon
     InformIcon "1" <-- "1" i
     ActivityCard "1" <-- "1" ContentWrap
     ContentWrap "1" <-- "1" Description
     ContentWrap "1" <-- "1" P6

     class DescriptionSideContent:::green
     class ActivityCard:::blue
     class Navbar
     class InformIcon
     class i
     class ContentWrap
     class Description
     class P6:::blue

`}
/>

</S.MermaidWrapper>

<ReferenceList
  tableRows={[
    {
      name: 'ActivityCard',
      description: 'A component that wraps its child into a card.',
    },
    {
      name: 'P6',
      description: 'This is a level 6 paragraph.',
    },
  ]}
/>
