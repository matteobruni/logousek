import { Canvas, Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as Button from './Button.stories'
import * as S from '../../styles.ts'
import PropertiesTable from '../../../Components/properties-table'
import ReferenceList from '../../../Components/reference-list'
import ComponentInfoTable from '../../../Components/component-info-table'

<Meta of={Button} />

# Button

<ComponentInfoTable
  name={'Button'}
  description="A Button component. When pressed, it invokes the onClick callback."
  source="components/Button"
/>

<PropertiesTable
  tableRows={[
    {
      name: 'children',
      type: `string | React.ReactNode`,
      defaultValue: '',
      description: `The value of the property is displayed as the button text. The text of the button can also be set via the title prop. If both props (title and children) have a value, the value from the children prop will be used`,
      required: false,
    },
    {
      name: 'title',
      type: `string`,
      defaultValue: '',
      description: `The value of the property is displayed as the button text. The text of the button can also be set via the title prop. If both props (title and children) have a value, the value from the children prop will be used`,
      required: false,
    },
    {
      name: 'onClick',
      type: `() => void`,
      defaultValue: '',
      description: `Callback that is called when the button is pressed.`,
      required: false,
    },

    {
      name: 'size',
      type: `xs | s`,
      defaultValue: '',
      description: `Sets the size of the button.`,
      required: false,
    },
        {
      name: 'disabled',
      type: `boolean`,
      defaultValue: '',
      description: `Sets the editability of the button.`,
      required: false,
    },
    {
      name: 'color',
      type: `string`,
      defaultValue: '',
      description: `Sets a text color.`,
      required: false,
    },
    {
      name: 'backgroundColor',
      type: `string`,
      defaultValue: '',
      description: `Sets a background color.`,
      required: false,
    },
    {
      name: 'type',
      type: `string`,
      defaultValue: '',
      description: `Sets a button type. If the button is used in a form, it is possible to set its type to "submit" this will cause the form to be sent after clicking the button or "reset" this will cause the form to be deleted after clicking.`,
      required: false,
    },
    {
      name: 'id',
      type: `string`,
      defaultValue: '',
      description: `Sets element identificator.`,
      required: false,
    },

]}
/>

<Canvas of={Button.Primary} withToolbar />

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
     Button "1" <-- "1" ButtonRowWrapper
     ButtonRowWrapper --|>  onClick : onClick

      class Button:::green
      class ButtonRowWrapper
      class onClick["props.onClick"]:::oval

`}
/>

</S.MermaidWrapper>

<ReferenceList tableRows={[]} />
