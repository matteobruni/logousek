import { Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../../../../styles.ts'
import PropertiesTable from '../../../../../Components/properties-table'
import ReferenceList from '../../../../../Components/reference-list'
import ComponentInfoTable from '../../../../../Components/component-info-table'
import ComponentRenderTable from '../../../../../Components/component-render'

<Meta title="ApplicationModel/Components/Sidebar/PageNavigation/ItemList/docs" />

# ItemList

<ComponentInfoTable
  name={'ItemList'}
  description={`This is a list of items for page navigation.`}
  source="components/side-bar/page-navigation/item-list"
/>

<PropertiesTable
  tableRows={[
    {
      name: 'itemList',
      type: `{
              name: string;
              icon: string;
              title: string;
              onClick: () => void;
            }[]`,
      defaultValue: '',
      description: `Data needed to display the items in list.`,
      required: true,
    },
    {
      name: 'setIsMenuShow',
      type: `(value: boolean) => void`,
      defaultValue: '',
      description: `A callback that sets the isMenuShow.`,
      required: true,
    },
    {
      name: 'isMenuShow',
      type: `boolean`,
      defaultValue: '',
      description: `Indicates whether the menu is open.`,
      required: true,
    },
  ]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
     ItemList "1" <-- "1" List
     List "1" <-- "n" Item

     class ItemList:::green
     class List
     class Item:::blue

`}
/>

</S.MermaidWrapper>

<ReferenceList
  tableRows={[
    {
      name: 'Item',
      description: 'This is a list item',
    },
  ]}
/>
