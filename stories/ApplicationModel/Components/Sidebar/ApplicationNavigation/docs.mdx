import { Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../../../styles.ts'
import PropertiesTable from '../../../../Components/properties-table'
import ReferenceList from '../../../../Components/reference-list'
import ComponentInfoTable from '../../../../Components/component-info-table'
import ComponentRenderTable from '../../../../Components/component-render'

<Meta title="ApplicationModel/Components/Sidebar/ApplicationNavigation/docs" />

# ApplicationNavigation

<ComponentInfoTable
  name={'ApplicationNavigation'}
  description={`This is a application navigation.`}
  source="components/side-bar/application-navigation"
/>

<PropertiesTable
  tableRows={[
    {
      name: 'applicationNav',
      type: `{
              name: string;
              icon: string;
              title: string;
              onClick: () => void;
            }`,
      defaultValue: '',
      description: `This is the menu configuration of the aplication menu.`,
      required: true,
    },
    {
      name: 'isMenuShow',
      type: `boolean`,
      defaultValue: '',
      description: `Indicates whether the menu is open.`,
      required: true,
    },
  ]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
     ApplicationNavigation "1" <-- "1" ApplicationNavigationWrapper
     ApplicationNavigationWrapper "1" <-- "1" H4
     
     ApplicationNavigationWrapper "1" <-- "1" ItemRectListWrapper
     ApplicationNavigationWrapper "1" <-- "1" DarkModeSwitchWrapper
     DarkModeSwitchWrapper "1" <-- "1" DarkModeSwitch

     ItemRectListWrapper "1" <-- "1" ItemRectList

     class ApplicationNavigation:::green
     class ApplicationNavigationWrapper
     class H4:::blue
     class ItemRectListWrapper
     class ItemRectList:::blue
     class DarkModeSwitchWrapper
     class DarkModeSwitch:::blue

`}
/>

</S.MermaidWrapper>

<ComponentRenderTable
  tableRows={[
    {
      name: 'DarkModeSwitchWrapper ',
      rules: `Rendered only if device width is 1024px or larger.`,
    },
  ]}
/>

<ReferenceList
  tableRows={[
    {
      name: 'DarkModeSwitch',
      description:
        'Switch component, which changes the theme of the application when the state changes.',
    },
    {
      name: 'ItemRectList',
      description: 'It is a list of items.',
    },
    {
      name: 'H4',
      description: 'It is a Level 4 Header component',
    },
  ]}
/>
