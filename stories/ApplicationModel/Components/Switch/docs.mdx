import { Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../../styles.ts'
import PropertiesTable from '../../../Components/properties-table'
import ReferenceList from '../../../Components/reference-list'
import ComponentInfoTable from '../../../Components/component-info-table'
import ComponentRenderTable from '../../../Components/component-render'

<Meta title="ApplicationModel/Components/Switch/docs" />

# Switch

<ComponentInfoTable
  name={'Switch'}
  description={`A two-state button component.`}
  source="components/switch"
/>

<PropertiesTable
  tableRows={[
    {
      name: 'enabledIcon',
      type: `React.ReactElement | string`,
      defaultValue: '',
      description: `icon that is displayed in the on-state.`,
      required: true,
    },
    {
      name: 'disabledIcon',
      type: `React.ReactElement | string`,
      defaultValue: '',
      description: `icon that is displayed in the off-state.`,
      required: true,
    },
    {
      name: 'onChange',
      type: `(value: boolean) => void`,
      defaultValue: '',
      description: `Callback that is called when the button is pressed.`,
      required: true,
    },
    {
      name: 'value',
      type: `boolean`,
      defaultValue: '',
      description: `Value`,
      required: true,
    },
  ]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
     SwitchWrapper "1" <-- "1" SwitchPointer
     SwitchWrapper "1" <-- "1" SwitchBackground

     class SwitchWrapper:::green
     class SwitchPointer
     class SwitchBackground

`}
/>

</S.MermaidWrapper>

<ReferenceList tableRows={[]} />
