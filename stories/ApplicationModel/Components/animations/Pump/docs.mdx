import { Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../../../styles.ts'
import PropertiesTable from '../../../../Components/properties-table'
import ReferenceList from '../../../../Components/reference-list'
import ComponentInfoTable from '../../../../Components/component-info-table'

<Meta title="ApplicationModel/Components/Animations/Pump/docs" />

# Pump

<ComponentInfoTable
  name={'Pump'}
  description="A pump animation component."
  source="components/animations/Pump"
/>

<PropertiesTable
  tableRows={[
     {
      name: 'children',
      type: `React.ReactElement`,
      defaultValue: '',
      description: `An element that should be animated`,
      required: true,
    },

]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
     Pump "1" <-- "1" Animate

     class Pump:::green
     class Animate:::blue

`}
/>

</S.MermaidWrapper>

<ReferenceList
  tableRows={[
    { name: 'Animate', description: 'Component that animates an object.' },
  ]}
/>
