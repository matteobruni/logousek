import { Mermaid } from 'mdx-mermaid/Mermaid'
import { Meta } from '@storybook/blocks'

import * as S from '../../../../styles.ts'
import appConf from '../../../../../../constants/app-conf'
import ComponentInfoTable from '../../../../../Components/component-info-table'
import ReferenceList from '../../../../../Components/reference-list'
import PropertiesTable from '../../../../../Components/properties-table'

<Meta title="ApplicationModel/Components/Pages/AdminPage/Paggination/docs" />

# Paggination

<ComponentInfoTable
  name="Paggination"
  description="The component displays pagination."
  source="components/pages/admin-pages/paggination"
/>

<PropertiesTable
  tableRows={[
    {
      name: 'maxIndexes',
      type: `number`,
      defaultValue: '',
      description: `This is the total number of all records.`,
      required: true,
    },
    {
      name: 'onLinkClick',
      type: `(index: number) => void`,
      defaultValue: '',
      description: `Callback that is called after the user clicks on the pagination element.`,
      required: true,
    },
    {
      name: 'currentList',
      type: `number`,
      defaultValue: '',
      description: `This is the active pagination page.`,
      required: true,
    },
  ]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
      Paggination "1" <-- "1" LinksWrapper
      LinksWrapper "1" <-- "n" Link

      class Paggination:::green
      class LinksWrapper
      class Link

`}
/>

</S.MermaidWrapper>

<ReferenceList tableRows={[]} />
