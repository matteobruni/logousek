import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../styles.ts'
import appConf from '../../../constants/app-conf'
import RouterTable from '../../Components/router-table'
import ReferenceList from '../../Components/reference-list'

# GameMenu

<RouterTable
  name="GameMenu"
  description="During the route, the user selects the activity and difficulty level they wish to play. Only logged in users can visit this page"
  source="components/pages/game-menu"
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
      GameMenu "1" <-- "1" GameMenuComponent

      class GameMenu:::green
      class GameMenuComponent["GameMenu"]:::blue

`}
/>

</S.MermaidWrapper>

<ReferenceList
  tableRows={[{ name: 'GameMenu', description: 'Game menu component.' }]}
/>
